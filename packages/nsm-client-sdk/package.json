{
  "name": "@nsm/client-sdk",
  "version": "0.1.0",
  "description": "NSM Client SDK - Main SDK for building NSM applications",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build:ts && bun run build:bundle",
    "build:ts": "tsc --build",
    "build:bundle": "bun build src/index.ts --outdir dist --format esm --outfile index.mjs && bun build src/index.ts --outdir dist --format cjs --outfile index.js",
    "dev": "bun run build:ts --watch",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nsm/core": "workspace:*",
    "@nsm/crypto": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "public"
  }
}